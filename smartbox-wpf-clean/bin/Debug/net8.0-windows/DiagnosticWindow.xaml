<Window x:Class="SmartBoxNext.DiagnosticWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Connection Diagnostics" 
        Height="700" 
        Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#0078d4" Padding="20">
            <TextBlock x:Name="HeaderText"
                       Text="DICOM Connection Diagnostics" 
                       Foreground="White" 
                       FontSize="18" 
                       FontWeight="Bold"/>
        </Border>
        
        <!-- Test Results -->
        <ScrollViewer Grid.Row="1" Margin="20">
            <StackPanel x:Name="TestResultsPanel">
                <!-- Test items will be added here dynamically -->
            </StackPanel>
        </ScrollViewer>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#f3f2f1" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="SummaryText" 
                           Grid.Column="0" 
                           VerticalAlignment="Center"
                           FontWeight="Bold"/>
                
                <Button x:Name="OkButton" 
                        Grid.Column="1"
                        Content="OK" 
                        Width="100" 
                        Height="32"
                        IsEnabled="False"
                        Click="OkButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>